import { Client, User } from '../types';

// This file is deprecated. 
// The application now uses a persistent local database service (services/db.ts).
// Access data via the AuthContext or direct DB calls.

export const MOCK_USERS: User[] = [];
export const MOCK_CLIENTS: Client[] = [];